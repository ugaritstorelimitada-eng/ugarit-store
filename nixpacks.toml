[phases.setup]
nixPkgs = ["nodejs-20_x", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @ugarit/medusa build"]

[start]
cmd = "pnpm --filter @ugarit/medusa start"
