{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm install --frozen-lockfile && sh -c \"cd storefront && next build\"",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "storefront/.next",
  "github": {
    "silent": true
  }
}
