{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd storefront && pnpm install && next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "storefront/.next",
  "github": {
    "silent": true
  }
}
